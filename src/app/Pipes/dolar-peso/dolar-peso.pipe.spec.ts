import { DolarPesoPipe } from './dolar-peso.pipe';

describe('DolarPesoPipe', () => {
  it('create an instance', () => {
    const pipe = new DolarPesoPipe();
    expect(pipe).toBeTruthy();
  });
});
