import { NumerosLetrasPipe } from './numeros-letras.pipe';

describe('NumerosLetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumerosLetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
